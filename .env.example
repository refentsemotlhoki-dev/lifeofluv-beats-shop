# LifeOfLuv storefront environment variables
# Copy this file to .env (local dev) and set the same key in Vercel project settings.

# Supabase Edge Function that creates a Stripe Checkout session.
# Defaults to the LifeOfLuv production function if unset, so this is optional,
# but set it explicitly to make deployments unambiguous.
VITE_CHECKOUT_FUNCTION_URL=https://jmespcsjkrucykzugsxn.supabase.co/functions/v1/create-checkout
